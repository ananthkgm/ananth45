#include<stdio.h>
int main()
{
char a[20],b[20];
int i,k;
printf("enter the name:");
scanf("%s",&a);
printf("enter the length:");
scanf("%d",&k);
  for(i=0;i<k;i++)
  {
    b[i]==a[i];
    printf("%s",b);
  }
  return 0;
}
